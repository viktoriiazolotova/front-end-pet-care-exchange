import React from "react";
import PetsList from "./PetsList";

const SelectedPetsitter = ({ selectedPetsitter }) => {
  return (
    <div>
      <p>Hi, here is detail info about me</p>
      {/* <p>{selectedPetsitter.name}</p> */}
      {/* <PetsList></PetsList> */}
    </div>
  );
};

export default SelectedPetsitter;
